// Example Firebase project config for the hosted console.
// To deploy with the real key: copy this file to firebase-config.js (which is
// gitignored) and fill in your values. index.html loads firebase-config.js
// before its own script and falls back to the in-page placeholder if it is
// missing (sign-in then fails loudly).
window.GP_FIREBASE = {
  apiKey: "REPLACE_WITH_YOUR_FIREBASE_API_KEY",
  projectId: "your-project-id",
  databaseUrl: "https://your-project-id-default-rtdb.firebaseio.com",
};
